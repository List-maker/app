import 'package:flutter/material.dart';

class MorphIn extends StatelessWidget {
  const MorphIn({required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return Container(child: child,); // TODO
  }
}
